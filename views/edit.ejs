<!DOCTYPE html>
<html lang="en">
<head>
    <% include partials/head.ejs %>
</head>
<body>
    <nav>
        <% include partials/nav.ejs %>
    </nav>
    <main class="container">
        <h2 class="flow-text center"><%= move.title %></h2>

        <div class="divider"></div>

        <div class="image-container center">
            <img class="edit-image" src="https://img.youtube.com/vi/<%= move.videoId %>/hqdefault.jpg" alt="YouTube thumbnail <%= move.title %>">
        </div>
    

        <form action="/techniques/<%= move.id%>?_method=PUT" method="POST">
            <!-- <label for="title">Title</label>
            <input type="text" name="title" id="title" value="<%= move.title %>">
    
            <label for="link">Embed Link</label>
            <input type="text" name="link" id="link" value="<%= move.link %>"> -->
    
            <label for="notes">Notes</label>
            <textarea name="notes" id="notes" cols="30" rows="30"><%= move.notes %></textarea>
    
            <!-- Submit -->
            <input class="btn" type="submit" value="Edit Technique">
        </form>

        <form action="/techniques/<%= move.id %>?_method=DELETE" method="POST">
            <input class="btn" type="submit" value="Delete Technique"/>
        </form>
    </main>
</body>
</html>